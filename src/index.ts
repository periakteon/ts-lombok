/**
 * Example function to demonstrate the package setup
 * @param name - The name to greet
 * @returns A greeting message
 */
export function greet(name: string): string {
    return `Hello, ${name}!`;
}

// Export version
export const version = '0.1.0';
